<UserControl x:Class="FestpunktDB.GUI.AttributeEditUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FestpunktDB.GUI"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="270"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="{Binding Title}" Grid.Column="0" HorizontalAlignment="Center"/>
        <TextBox x:Name="CurInput" Grid.Column="1" HorizontalAlignment="Center" MinWidth="90" VerticalAlignment="Center" IsEnabled="False"/>
        <TextBox x:Name="NewInput" Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center" MinWidth="90"/>
    </Grid>
</UserControl>
